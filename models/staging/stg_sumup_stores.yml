version: 2
models:
  - name: stg_sumup_stores
    description: Table containing all sumup stores. 

    columns:
      - name: store_id
        description: Unique ID of a store.
        tests:
          - not_null
          - unique
          