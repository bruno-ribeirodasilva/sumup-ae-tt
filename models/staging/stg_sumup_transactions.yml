version: 2
models:
  - name: stg_sumup_transactions
    description: Table containing all sumup transactions. 

    columns:
      - name: transaction_id
        description: Unique ID of a single transaction
        tests:
          - not_null
          - unique
